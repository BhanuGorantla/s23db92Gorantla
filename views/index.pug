extends layout

block content
  h1= title
  p i love to smile 
  button(type="button", onclick="goFaster()", class="btn btn-success") Press Me
  div#faster Increase the speed
  script.
    var mph = 20;
    var change = 1;
    function goFaster() {
      mph += change;
      change += 1;
      document.getElementById('faster').textContent = "MPH " + mph + " with change " + change;
    }
  a(href="/bikes") bikes